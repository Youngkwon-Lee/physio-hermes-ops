Run the nightly second-brain capture pass on the home desktop.

Goal: collect newly available signals from mobile captures, screenshots, ChatGPT/Codex conversation captures, Hermes/Discord handoff surfaces, Gmail candidates, and existing raw delta folders, then place them in the correct second-brain layer without dumping private raw archives into canonical docs.

Home desktop paths:
- Windows workspace: `C:\Users\82106\Documents\brain`
- WSL alias: `/home/yk/brain`
Use the path exposed by the current runtime, but do not use MacBook paths.

Required workflow:
1. Read `INDEX.md`, `README.md`, `operations/ingest-checklist.md`, `operations/mobile-capture-operating-rule-v1.md`, `operations/chatgpt-screen-capture-routing-v1.md`, `operations/source-ingest-registry.md`, `operations/second-brain-automation-map.md`, `operations/second-brain-runtime-topology-v1.md`, `operations/automation-control-center.md`, and `operations/promotion-and-sync-policy.md` before editing.
2. Check `git status --short --branch`. If the worktree is clean, run `git fetch --all --prune` and then only fast-forward pull the current branch. If the worktree is dirty or the pull is not fast-forwardable, do not force anything; continue read-only where possible and report the sync blocker.
3. Inspect only recent/new material, prioritizing the last 24 hours and registry deltas under `_inbox/mobile/`, `operations/raw/mobile-note-deltas/`, `operations/raw/mobile-screenshot-deltas/`, `operations/raw/apple-notes-inbox-deltas/`, `operations/raw/google-keep-deltas/`, `operations/raw/codex-thread-deltas/`, `operations/raw/continuity/`, `operations/raw-handoff-digests/`, `operations/candidates/`, `candidates/`, and `candidates/captures/` (iOS Back Tap screenshot OCR captures committed directly by the brain-capture Cloudflare Worker).
4. For ChatGPT archive content, search narrowly only when a new exported ChatGPT item or explicit pointer exists. Do not scan or mirror the full archive.
5. For ChatGPT Chrome screen captures or private conversation captures, follow `operations/chatgpt-screen-capture-routing-v1.md`: first classify importance using title/URL/recent excerpt; save full raw turns only for high-importance captures or explicit user requests; for medium captures save Summary / Decisions / Next Actions / Reusable Concepts only; for low captures save metadata only or ignore. Nightly review should read summary/decision/action blocks first and treat raw turns as fallback evidence.
6. For mobile screenshots, preserve the privacy boundary: use metadata/OCR/candidate summaries when available, do not store raw images in git unless an existing capture tool already created an allowed candidate. iOS Back Tap captures already arrive as OCR markdown under `candidates/captures/` (raw PNG stays in Cloudflare R2, never in git) — treat each `.md` as a raw candidate to triage, distill, and route; do not pull the R2 image into git.
7. Route each useful signal to the smallest correct writeback: update a mobile/source index, create or update one candidate note, or make a small canonical patch only when the target is obvious and stable. Do not dump full transcripts, secrets, tokens, PHI, or unsanitized screenshots.
8. If changing files, run `python3 operations/tools/brain_lint.py` or the available Python equivalent, then `git diff --check`. Summarize pass/fail with exact failing command if any.
9. If changes are generated by this automation and checks pass, commit them with a concise message like `chore: nightly second-brain capture YYYY-MM-DD` and push the current branch. If there are pre-existing unrelated user changes, do not stage or commit them; report what blocked auto-commit.
10. Final output should include: sources checked, files changed, candidate/canonical destinations, skipped sensitive material, validation results, git sync result, and ChatGPT capture importance decisions if any.
